{
	"name": "typescript-client-template",
	"version": "1.0.0",
	"description": "",
	"main": "./src/test-app.ts",
    "type": "module",
	"scripts": {
		"dev": "npm run build && tsc --watch",
		"build": "npm run build:remove-dist && tsc && npm run build:copy-statics",
        "build:remove-dist": "rm -rf ./dist",
		"build:copy-statics": "cp -r ./src/** ./dist"
	},
	"author": "@elerium115",
	"license": "ISC"
}
